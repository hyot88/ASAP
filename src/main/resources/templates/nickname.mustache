{{>layout/header}}
    <h1>닉네임을 정해보쟈!</h1>
    <div class="col-md-12">
        <div class="row">
            <div class="col-md-6">
                <input id="nickname" type="text" value="{{userName2}}">
                <button id="btn-nickname" class="btn btn-primary">닉네임 결정</button>
            </div>
        </div>
    </div>
{{>layout/footerNickname}}